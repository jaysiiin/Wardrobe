@model IEnumerable<Wardrobe1.Models.ShoeStuff>

@{
    ViewBag.Title = "Index";
}

<h2>Shoes</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Photo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Color)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Season)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Occasion)
        </th>
        <th></th>
    </tr>*@
<div class="row">
    @foreach (var item in Model)
    {

        <div class="col-xs-6 col-md-3">
            <div class="thumbnail">
                @Html.DisplayFor(modelItem => item.Name)
                <img src="@Url.Content(item.Photo)" />
                <p>
                    <br />
                    <div class="btn btn-primary" role="button">
                        @Html.ActionLink("Edit", "Edit", new { id = item.ShoeID }, new { id = "crudLink" })
                    </div>
                    <div class="btn btn-primary" role="button">
                        @Html.ActionLink("Details", "Details", new { id = item.ShoeID }, new { id = "crudLink" })
                    </div>
                    <div class="btn btn-primary" role="button">
                        @Html.ActionLink("Delete", "Delete", new { id = item.ShoeID }, new { id = "crudLink" })
                    </div>
                </p>
            </div>
        </div>
    }
</div>